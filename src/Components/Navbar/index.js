import styled from "styled-components";

const NavbarWrapper = styled.section`
  height: 3rem;
  width: 100%;
`;

const Navbar = (props) => {
  return <NavbarWrapper>nav bar</NavbarWrapper>;
};

export default Navbar;
